import { GhostLoadingDirective } from './ghost-loading.directive';

describe('GhostLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new GhostLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
